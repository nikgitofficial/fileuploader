import React from 'react';
import { Container, Typography } from '@mui/material';

const Files = () => {
  return (
    <Container sx={{ mt: 4 }}>
      <Typography variant="h4">Your Files Page</Typography>
      <Typography>File listing, filtering, etc. will go here.</Typography>
    </Container>
  );
};

export default Files;
